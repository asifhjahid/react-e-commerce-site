import styled from "styled-components";


const ServiceListStyle=styled.div` 

            padding: 60px 0;
            .serviceArea {
                    display: grid;
                    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
                    grid-gap: 15px;
                    }


`
export default ServiceListStyle;